export enum ThemeMode {
  DARK = 'dark',
  LIGHT = 'light',
};

export enum LanguageMode {
  PT = 'pt',
  EN = 'en'
}

export enum RoutesENUM {
  HOME = "/home",
  BLOG = "/blog",
  ABOUT_ME = "/about-me",
  CONTACT = "/contact"
}